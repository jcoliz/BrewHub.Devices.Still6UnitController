# Required parameters
# - userName: Registry organization/user prefix

variables:
  Solution.SourceDirectory: '$(System.DefaultWorkingDirectory)'
  Container.Name: '$(userName)/brewhub-still6unit-controller'
  Container.Platform: 'amd64'
